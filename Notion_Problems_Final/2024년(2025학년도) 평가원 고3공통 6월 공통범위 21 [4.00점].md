## 문제
최고차항의 계수가 `$1$`인 사차함수 `$f(x)$`가 다음 조건을 만족시킨다.

(가) `$f'(a) \le 0$`인 실수 `$a$`의 최댓값은 `$2$`이다.  
(나) 집합 `$\{x | f(x)=k\}$`의 원소의 개수가 `$3$` 이상이 되도록 하는 실수 `$k$`의 최솟값은 `$\frac{8}{3}$`이다.  

`$f(0)=0$`, `$f'(1)=0$`일 때, `$f(3)$`의 값을 구하시오.

## 풀이
[정답] `$15$`

조건 (나)에서 집합 `$\{x | f(x)=k\}$`의 원소의 개수가 `$3$` 이상이 되도록 하는 실수 `$k$`의 값이 존재하므로 사차함수 `$f(x)$`는 극값을 `$3$`개 가진다.  
조건 (가)에서 `$f'(a) \le 0$`인 실수 `$a$`의 최댓값이 `$2$`이므로 `$f'(2)=0$`이다.  
`$f'(1)=0$`이므로 방정식 `$f'(x)=0$`의 다른 한 근을 `$t$`라 하면 함수 `$y=f'(x)$`의 개형은 다음 그림과 같다.  

> [!example] 📸 (그림/그래프)

이때 `$f(0)=0$`이므로 함수 `$y=f(x)$`의 개형은 다음과 같다.  

> [!example] 📸 (그림/그래프)

따라서 `$y=f(x)$`의 그래프와 직선 `$y=\frac{8}{3}$`이 접하므로 방정식 `$f(x)=\frac{8}{3}$`을 만족하는 다른 두 근을 `$\alpha$`, `$\beta$`라 하면  

`$f(x) = (x-\alpha)(x-\beta)(x-2)^{2} + \frac{8}{3}$` $\cdots$ ㉠  

`$f(0)=0$`이므로 ㉠에 대입하면  

`$4\alpha\beta + \frac{8}{3} = 0$`  
$\therefore \alpha\beta = -\frac{2}{3}$ $\cdots$ ㉡  

`$f'(x) = (x-\beta)(x-2)^{2} + (x-\alpha)(x-2)^{2} + 2(x-\alpha)(x-\beta)(x-2)$`  

`$f'(1)=0$`이므로  

`$f'(1) = 1-\beta + 1-\alpha - 2(1-\alpha-\beta+\alpha\beta)$`  
`$= \alpha + \beta - 2\alpha\beta$`  
`$= \alpha + \beta + \frac{4}{3} = 0$`  

$\therefore \alpha + \beta = -\frac{4}{3}$ $\cdots$ ㉢  

$\therefore f(3) = (3-\alpha)(3-\beta)(3-2)^{2} + \frac{8}{3}$  
`$= 9 - 3(\alpha + \beta) + \alpha\beta + \frac{8}{3}$`  
`$= 9 - 3 \times \left(-\frac{4}{3}\right) - \frac{2}{3} + \frac{8}{3}$` ($\because$ ㉡, ㉢)  
`$= 15$`
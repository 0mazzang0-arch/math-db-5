## 문제
이차함수 $f(x) = (x - \alpha)(x - \beta)$에서 두 상수 $\alpha$, $\beta$가 다음 조건을 만족시킨다.

(가) $\alpha < 0 < \beta$
(나) $\alpha + \beta > 0$

이때, 세 정적분 
$A = \int_{\alpha}^{0} f(x) dx$, $B = \int_{0}^{\beta} f(x) dx$, $C = \int_{\alpha}^{\beta} f(x) dx$
의 값의 대소 관계를 바르게 나타낸 것은?

## 보기
① $A < B < C$   ② $A < C < B$   ③ $B < A < C$   ④ $C < A < B$   ⑤ $C < B < A$

## 풀이
[정답] ⑤ 

이차함수 $f(x)$의 그래프는 그림과 같다. 

> [!example] 📸 (그림/그래프)

$\therefore C < B < A$
## 문제

함수 $f(x)=x^{2}+ax+b$ 에 대하여 함수
$$ g(x)= \begin{cases} | f(x)| -x^{2} & (x \le 0) \\ \{f(x)\}^{2}+x^{3} & (x > 0) \end{cases} $$
이 다음 조건을 만족시킨다.

(가) 함수 $g(x)$ 는 $x=b$ 에서만 미분가능하지 않다.
(나) 방정식 $g(x)=0$ 은 음의 실근을 갖는다.

$g(-\frac{1}{2})+g(3)$ 의 값은? (단, $a$, $b$ 는 상수이다.)

## 보기

① $\frac{183}{2}$   ② $\frac{187}{2}$   ③ $\frac{191}{2}$   ④ $\frac{195}{2}$   ⑤ $\frac{199}{2}$

## 풀이

[정답] ①

$$ g(x)= \begin{cases} | x^{2}+ax+b| -x^{2} & (x \le 0) \\ (x^{2}+ax+b)^{2}+x^{3} & (x > 0) \end{cases} $$
구간 $(0, \infty)$ 에서 함수 $g(x)$ 는 미분가능하므로 조건 (가)에 의하여 $b \le 0$ 이다.

(ⅰ) $b=0$ 인 경우
$f(x)=x^{2}+ax=x(x+a)$ 이므로
$$ g(x)= \begin{cases} | x(x+a)| -x^{2} & (x \le 0) \\ x^{2}(x+a)^{2}+x^{3} & (x > 0) \end{cases} $$

① $a > 0$ 인 경우
$$ g(x)= \begin{cases} ax & (x \le -a) \\ -2x^{2}-ax & (-a < x \le 0) \\ x^{2}(x+a)^{2}+x^{3} & (x > 0) \end{cases} $$
$x=-a$ 에서의 미분가능성을 조사하면
$\lim_{x \to -a-} \frac{g(x)-g(-a)}{x-(-a)} = \lim_{x \to -a-} \frac{ax-(-a^{2})}{x+a}=a$
$\lim_{x \to -a+} \frac{g(x)-g(-a)}{x-(-a)} = \lim_{x \to -a+} \frac{(-2x^{2}-ax)-(-a^{2})}{x+a} = \lim_{x \to -a+} \frac{-(2x-a)(x+a)}{x+a}=3a$
$a > 0$ 이므로 $a \ne 3a$ 이다.
$g(x)$ 는 $x=-a$ 에서 미분가능하지 않으므로 조건 (가)를 만족시키지 않는다.

② $a=0$ 인 경우
$$ g(x)= \begin{cases} 0 & (x \le 0) \\ x^{4}+x^{3} & (x > 0) \end{cases} $$
$x=0$ 에서의 미분가능성을 조사하면
$\lim_{x \to 0-} \frac{g(x)-g(0)}{x} = \lim_{x \to 0-} \frac{0-0}{x}=0$
$\lim_{x \to 0+} \frac{g(x)-g(0)}{x} = \lim_{x \to 0+} \frac{x^{4}+x^{3}-0}{x}=0$
$\lim_{x \to 0-} \frac{g(x)-g(0)}{x} = \lim_{x \to 0+} \frac{g(x)-g(0)}{x}$
$g(x)$ 는 $x=0$ 에서 미분가능하므로 조건 (가)를 만족시키지 않는다.

③ $a < 0$ 인 경우
$$ g(x)= \begin{cases} ax & (x \le 0) \\ x^{2}(x+a)^{2}+x^{3} & (x > 0) \end{cases} $$
$x < 0$ 인 모든 실수 $x$ 에 대하여 $g(x)=ax \ne 0$ 이므로 조건 (나)를 만족시키지 않는다.

(ⅱ) $b < 0$ 인 경우
$g(x)$ 는 $x=0$ 에서 미분가능하므로 연속이다.
$\lim_{x \to 0-} g(x) = \lim_{x \to 0+} g(x) = g(0)$
$\lim_{x \to 0-} g(x) = |b| = -b$
$\lim_{x \to 0+} g(x) = b^{2}$
$g(0) = |b| = -b$
$-b=b^{2}$ 에서 $b < 0$ 이므로 $b=-1$ 이고, 함수 $g(x)$ 는 $x=-1$ 에서만 미분가능하지 않다.
$$ g(x)= \begin{cases} | x^{2}+ax-1| -x^{2} & (x \le 0) \\ (x^{2}+ax-1)^{2}+x^{3} & (x > 0) \end{cases} $$
$x^{2}+ax-1=0$ 의 서로 다른 두 실근을 $\alpha$, $\beta$ $(\alpha < \beta)$ 라 하면, 근과 계수의 관계에 의하여 $\alpha\beta = -1$ 이므로 $\alpha < 0 < \beta$ 이다.
$$ g(x)= \begin{cases} ax-1 & (x \le \alpha) \\ -2x^{2}-ax+1 & (\alpha < x \le 0) \\ (x^{2}+ax-1)^{2}+x^{3} & (x > 0) \end{cases} $$
$g(x)$ 는 $x=0$ 에서 미분가능해야 하므로
$\lim_{x \to 0-} \frac{g(x)-g(0)}{x} = \lim_{x \to 0-} \frac{(-2x^{2}-ax+1)-1}{x} = -a$
$\lim_{x \to 0+} \frac{g(x)-g(0)}{x} = \lim_{x \to 0+} \frac{\{(x^{2}+ax-1)^{2}+x^{3}\}-1}{x} = -2a$
$-a = -2a$ 에서 $a=0$ 이다.
따라서 $g(x)= \begin{cases} | x^{2}-1| -x^{2} & (x \le 0) \\ (x^{2}-1)^{2}+x^{3} & (x > 0) \end{cases}$ 이다.
$g(-\frac{1}{2})+g(3) = \frac{1}{2}+91 = \frac{183}{2}$

[참고]
$f(x)=x^{2}-1$ 일 때,
$$ g(x)= \begin{cases} -1 & (x \le -1) \\ -2x^{2}+1 & (-1 < x \le 0) \\ x^{4}+x^{3}-2x^{2}+1 & (x > 0) \end{cases} $$
함수 $y=g(x)$ 의 그래프는 다음과 같다.

> [!example] 📸 (그림/그래프)
## 문제
좌표평면에서 원 $x^{2}+y^{2}=r^{2}$ ($r>0$) 과 포물선 $y^{2}=x$ 의 교점 중 제 $1$ 사분면 위에 있는 점을 $P$ 라 하고, 두 점 $P$, $Q(0, r)$ 를 지나는 직선이 $x$ 축과 만나는 점을 $R$ 라 하자. 다음은 $r$ 의 값이 $0$ 에 한없이 가까워질 때, 점 $R$ 가 한없이 가까워지는 점의 좌표를 구하는 과정이다.

> [!example] 📸 (그림/그래프)
> 
> 선분 $OP$ 와 $x$ 축이 이루는 각의 크기를 $\theta$ 라 하면 $P$ 는 원 $x^{2}+y^{2}=r^{2}$ 위의 점이므로 $P(r \cos \theta, r \sin \theta)$ 로 놓을 수 있다.
> 
> $P$ 는 $y^{2}=x$ 위의 점이므로
> $r = (\text{가})$ 이다. $\cdots$ ㉠
> 
> $P(r \cos \theta, r \sin \theta)$, $Q(0, r)$ 를 지나는 직선의 방정식은
> $y = \frac{\sin \theta - 1}{\cos \theta} x + r$ 이다.
> 
> $R$ 의 좌표를 $R(a, 0)$ 으로 놓으면
> $a = \frac{r \cos \theta}{1 - \sin \theta}$ 이다. $\cdots$ ㉡
> 
> $r \to 0$ 일 때, $\theta \to \frac{\pi}{2}$ 이므로 ㉠, ㉡에서
> $\lim_{r \to 0} a = \lim_{\theta \to \frac{\pi}{2}} \frac{r \cos \theta}{1 - \sin \theta} = (\text{나})$ 이다.
> 
> $r$ 의 값이 $0$ 에 한없이 가까워질 때, $R$ 는 점 $((\text{나}), 0)$ 에 한없이 가까워진다.

위 과정에서 (가), (나)에 알맞은 것은?

## 보기
① $\text{(가)}: \frac{\cos \theta}{\sin \theta}, \text{(나)}: 1$   ② $\text{(가)}: \frac{\cos \theta}{\sin \theta}, \text{(나)}: 2$   ③ $\text{(가)}: \frac{\cos \theta}{\sin^{2} \theta}, \text{(나)}: 1$   ④ $\text{(가)}: \frac{\cos \theta}{\sin^{2} \theta}, \text{(나)}: 2$   ⑤ $\text{(가)}: \frac{\cos \theta}{\sin^{2} \theta}, \text{(나)}: 4$

## 풀이
정답: ④

(가) : 점 $P(r \cos \theta, r \sin \theta)$ 가 포물선 $y^{2}=x$ 위의 점이므로
$$ (r \sin \theta)^{2} = r \cos \theta $$
$$ r^{2} \sin^{2} \theta = r \cos \theta $$
$r > 0$ 이므로 양변을 $r \sin^2 \theta$ 로 나누면
$$ r = \frac{\cos \theta}{\sin^{2} \theta} $$

(나) : ㉠을 ㉡에 대입하여 극한값을 구하면
$$ \lim_{\theta \to \frac{\pi}{2}} \frac{r \cos \theta}{1 - \sin \theta} = \lim_{\theta \to \frac{\pi}{2}} \frac{\cos^{2} \theta}{\sin^{2} \theta (1 - \sin \theta)} $$
$$ = \lim_{\theta \to \frac{\pi}{2}} \frac{1 - \sin^{2} \theta}{\sin^{2} \theta (1 - \sin \theta)} $$
$$ = \lim_{\theta \to \frac{\pi}{2}} \frac{(1 - \sin \theta)(1 + \sin \theta)}{\sin^{2} \theta (1 - \sin \theta)} $$
$$ = \lim_{\theta \to \frac{\pi}{2}} \frac{1 + \sin \theta}{\sin^{2} \theta} = \frac{1 + 1}{1^{2}} = 2 $$

따라서 $(\text{가}) = \frac{\cos \theta}{\sin^{2} \theta}$, $(\text{나}) = 2$ 이다.
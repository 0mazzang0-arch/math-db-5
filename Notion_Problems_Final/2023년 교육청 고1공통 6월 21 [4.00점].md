## 문제

> [!example] 📸 (그림/그래프)

`$1$`이 아닌 양수 `$k$`에 대하여 직선 `$y=k$`와 이차함수 `$y=x^{2}$`의 그래프가 만나는 두 점을 각각 `$A$`, `$B$`라 하고, 직선 `$y=k$`와 이차함수 `$y=x^{2}-6x+6$`의 그래프가 만나는 두 점을 각각 `$C$`, `$D$`라 할 때, <보기>에서 옳은 것만을 있는 대로 고른 것은?

**<보기>**
ㄱ. `$k=6$`일 때, `$\overline{CD}=6$`이다.
ㄴ. `$k$`의 값에 관계없이 `${\overline{CD}}^{2} - {\overline{AB}}^{2}$`의 값은 일정하다.
ㄷ. `$\overline{CD} + \overline{AB} = 4$`일 때, `$k + \overline{BC} = \frac{17}{16}$`이다.

(단, 점 `$A$`의 `$x$`좌표는 점 `$B$`의 `$x$`좌표보다 작고, 점 `$C$`의 `$x$`좌표는 점 `$D$`의 `$x$`좌표보다 작다.)

## 보기

① $ㄱ$   ② $ㄴ$   ③ $ㄱ, ㄴ$   ④ $ㄴ, ㄷ$   ⑤ $ㄱ, ㄴ, ㄷ$

## 풀이

ㄱ. 방정식 `$x^{2}-6x+6=6$`의 해는 `$x=0$` 또는 `$x=6$`이므로 점 `$C(0, 6)$`, 점 `$D(6, 6)$`에서 `$\overline{CD}=6$`이다. (참)

ㄴ. 방정식 `$x^{2}=k$`의 해는 `$x = \pm \sqrt{k}$`이므로 점 `$A(-\sqrt{k}, k)$`, 점 `$B(\sqrt{k}, k)$`에서 `${\overline{AB}}^{2} = 4k$`
`$C$`, `$D$`의 `$x$`좌표를 각각 `$\alpha$`, `$\beta$`라 하면 방정식 `$x^{2}-6x+6=k$`에서 근과 계수의 관계에 의해
`$\alpha + \beta = 6$`, `$\alpha \beta = 6 - k$`
`${\overline{CD}}^{2} = (\beta - \alpha)^{2} = (\alpha + \beta)^{2} - 4\alpha\beta = 6^{2} - 4(6-k) = 36 - 24 + 4k = 12 + 4k$`
따라서 `${\overline{CD}}^{2} - {\overline{AB}}^{2} = (12+4k) - 4k = 12$`로 일정하다. (참)

ㄷ. `${\overline{CD}}^{2} - {\overline{AB}}^{2} = (\overline{CD} + \overline{AB})(\overline{CD} - \overline{AB}) = 12$`에서
`$\overline{CD} + \overline{AB} = 4$`이므로 `$\overline{CD} - \overline{AB} = 3$`
두 식을 연립하면 `\overline{AB} = \frac{1}{2}`, `\overline{CD} = \frac{7}{2}`이다.
`$\overline{AB} = 2\sqrt{k} = \frac{1}{2}$`에서 `$\sqrt{k} = \frac{1}{4}$`이므로 `$k = \frac{1}{16}$`이다.
점 `$B$`의 `$x$`좌표는 `$\sqrt{k} = \frac{1}{4}$`이고,
`$x^{2}-6x+6 = \frac{1}{16}$`에서 `$16x^{2}-96x+95=0$`이므로 `$(4x-5)(4x-19)=0$`
`$x = \frac{5}{4}$` 또는 `$x = \frac{19}{4}$`이다.
점 `$C$`의 `$x$`좌표는 점 `$D$`의 `$x$`좌표보다 작으므로 점 `$C$`의 `$x$`좌표는 `$\frac{5}{4}$`이다.
이때 `$\overline{BC} = (\text{점 } C \text{의 } x\text{좌표}) - (\text{점 } B \text{의 } x\text{좌표}) = \frac{5}{4} - \frac{1}{4} = 1$`
따라서 `$k + \overline{BC} = \frac{1}{16} + 1 = \frac{17}{16}$`이다. (참)

정답: ⑤
## 문제
두 집합 
$A = \{ x \mid x^{2} - ( a+b )x + ab < 0 \}$, 
$B = \{ x \mid 2^{2x+2} - 9 \cdot 2^{x} + 2 < 0 \}$ 
에 대하여 $A \subset B$ 일 때, $b-a$ 의 최댓값을 구하시오. (단, $a < b$ 이다.)



> [!example] 📸 (그림/그래프)

## 풀이
$x^{2} - (a+b)x + ab < 0$
$\Leftrightarrow (x-a)(x-b) < 0$
$a < b$ 이므로 
$a < x < b$
$\therefore A = \{ x \mid a < x < b \}$

한편, 
$2^{2x+2} - 9 \cdot 2^{x} + 2 < 0$
$\Leftrightarrow 4(2^{x})^{2} - 9 \cdot 2^{x} + 2 < 0$
$\Leftrightarrow (4 \cdot 2^{x} - 1)(2^{x} - 2) < 0$
$\Leftrightarrow \frac{1}{4} < 2^{x} < 2$
$\Leftrightarrow 2^{-2} < 2^{x} < 2^{1}$
$\therefore B = \{ x \mid -2 < x < 1 \}$

$A \subset B$ 이므로 
$-2 \le a < b \le 1$ 
따라서 $b-a$ 의 최댓값은 
$1 - (-2) = 3$

정답: $3$
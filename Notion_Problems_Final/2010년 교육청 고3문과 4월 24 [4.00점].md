## 문제
> [!example] 📸 (그림/그래프)

다음 순서도에서 인쇄되는 `$S$`의 값을 구하시오.

## 풀이
`$n=1$` 일 때, 
`$S=20+17>0$` 

`$n=2$` 일 때, 
`$S=20+17+14>0$` 

`$n=3$` 일 때, 
`$S=20+17+14+11>0$` 

`$\vdots$` 

`$n=13$` 일 때, 
`$S=20+17+\cdots+(-19)=7>0$` 

`$n=14$` 일 때, 
`$S=20+17+\cdots+(-22)=-15<0$` 

이므로 
`$|S|+n=15+14=29$` 이다. 

따라서, 인쇄되는 `$S$`의 값은 `$29$` 이다.
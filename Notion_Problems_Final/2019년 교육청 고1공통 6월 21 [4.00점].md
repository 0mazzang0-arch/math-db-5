## 문제

두 이차함수 $f(x) = (x-a)^{2} - a^{2}$, $g(x) = -(x-2a)^{2} + 4a^{2} + b$가 다음 조건을 만족시킨다.

$$ \begin{array}{|l|} \hline \text{(가) 방정식 } f(x) = g(x) \text{는 서로 다른 두 실근 } \alpha, \beta \text{를 갖는다.} \\ \text{(나) } \beta - \alpha = 2 \\ \hline \end{array} $$

보기에서 옳은 것만을 있는 대로 고른 것은? (단, $a, b$는 상수이다.)



> [!example] 📸 (그림/그래프)

## 보기

ㄱ. $a=1$ 일 때, $b = -\frac{5}{2}$이다.
ㄴ. $f(\beta) - g(\alpha) \le g(2a) - f(a)$
ㄷ. $g(\beta) = f(\alpha) + 5a^{2} + b$ 이면 $b = -16$이다.

① ㄱ   ② ㄴ   ③ ㄱ, ㄴ   ④ ㄴ, ㄷ   ⑤ ㄱ, ㄴ, ㄷ

## 풀이

ㄱ. $a=1$ 이므로
$f(x) = (x-1)^{2} - 1 = x^{2} - 2x$,
$g(x) = -(x-2)^{2} + 4 + b = -x^{2} + 4x + b$
$x^{2} - 2x = -x^{2} + 4x + b$ 에서 $2x^{2} - 6x - b = 0$
$\beta = \alpha + 2$ 이므로 이차방정식 $2x^{2} - 6x - b = 0$은 두 근 $\alpha, \alpha+2$를 갖는다.
근과 계수의 관계에 의하여
$\alpha + (\alpha + 2) = 3$, $\alpha(\alpha + 2) = -\frac{b}{2}$
$\alpha + (\alpha + 2) = 3$ 에서 $2\alpha = 1$ 이므로 $\alpha = \frac{1}{2}$이고,
$\alpha(\alpha + 2) = \frac{1}{2} \times \frac{5}{2} = \frac{5}{4}$ 이므로 $-\frac{b}{2} = \frac{5}{4}$이다.
따라서 $b = -\frac{5}{2}$이다. (참)

ㄴ. $f(x) = (x-a)^{2} - a^{2}$ 이므로 $f(x)$의 최솟값은 $f(a) = -a^{2}$이다.
$g(x) = -(x-2a)^{2} + 4a^{2} + b$ 이므로 $g(x)$의 최댓값은 $g(2a) = 4a^{2} + b$이다.
$f(\alpha) = g(\alpha)$, $f(\beta) = g(\beta)$ 이므로 두 이차함수 $f(x), g(x)$의 그래프는 서로 다른 두 점 $(\alpha, f(\alpha)), (\beta, f(\beta))$에서 만난다.
$f(x), g(x)$의 그래프가 서로 다른 두 점에서 만나므로 $g(2a) > f(a)$이다.
$a$의 값에 따라 세 가지 경우로 나누어 생각할 수 있다.

(ⅰ) $a < 0$ 인 경우
$f(\beta) - g(\alpha) = f(\beta) - f(\alpha) < g(2a) - f(a)$

(ⅱ) $a = 0$ 인 경우
$f(\beta) - g(\alpha) = f(\beta) - f(\alpha) < g(2a) - f(a)$

(ⅲ) $a > 0$ 인 경우
$f(\beta) - g(\alpha) = f(\beta) - f(\alpha) \le g(2a) - f(a)$

따라서 모든 경우에 대하여 $f(\beta) - g(\alpha) \le g(2a) - f(a)$가 성립한다. (참)

ㄷ. $g(\beta) = f(\alpha) + 5a^{2} + b$ 에서 $g(\beta) = f(\beta)$ 이므로
$f(\beta) - f(\alpha) = 5a^{2} + b$ 이다.
이때 $g(2a) - f(a) = 4a^{2} + b - (-a^{2}) = 5a^{2} + b$ 이므로
$f(\beta) - f(\alpha) = g(2a) - f(a) \cdots$ ①
ㄴ의 과정에 의해 ①이 성립하려면 $a > 0$ 이고 $\alpha = a, \beta = 2a$ 이어야 한다.
$\beta - \alpha = 2a - a = a$ 이고 조건 (나)에서 $\beta - \alpha = 2$ 이므로 $a = 2$ 이다.
방정식 $f(x) = g(x)$, 즉 $2x^{2} - 6ax - b = 0$ 의 두 근의 곱은 $\alpha\beta = -\frac{b}{2}$ 이다.
$\alpha = 2, \beta = 4$ 이므로 $2 \times 4 = -\frac{b}{2}$ 에서 $b = -16$ 이다. (참)

[다른 풀이]
ㄱ. 방정식 $f(x) = g(x)$ 에서 $2x^{2} - 6ax - b = 0$ 의 두 실근이 $\alpha, \beta$ 이므로
$\alpha + \beta = 3a$, $\alpha\beta = -\frac{b}{2}$ 이다.
$(\beta - \alpha)^{2} = (\alpha + \beta)^{2} - 4\alpha\beta$ 이므로
$2^{2} = (3a)^{2} - 4 \times (-\frac{b}{2})$, 즉 $9a^{2} + 2b = 4 \cdots$ ㉠
$a=1$ 을 ㉠에 대입하면 $9 + 2b = 4$, $b = -\frac{5}{2}$ 이다.

ㄴ. $f(x) \ge f(a) = -a^{2}$ 이고 $g(x) \le g(2a) = 4a^{2} + b$ 이다.
$g(\beta) - f(\alpha) \le g(2a) - f(a)$ 이고 $g(\beta) = f(\beta)$ 이므로
$f(\beta) - g(\alpha) = g(\beta) - f(\alpha) \le g(2a) - f(a)$ 이다.

ㄷ. $g(\beta) = f(\alpha) + 5a^{2} + b$ 에서 $g(\beta) - f(\alpha) = 5a^{2} + b$ 이다.
$g(2a) - f(a) = 4a^{2} + b - (-a^{2}) = 5a^{2} + b$ 이므로
$g(\beta) - f(\alpha) = g(2a) - f(a)$ 이다.
이는 $\beta = 2a, \alpha = a$ 일 때 성립한다.
(나)에서 $\beta - \alpha = 2$ 이므로 $2a - a = 2$, 즉 $a = 2$ 이다.
㉠에 $a = 2$ 를 대입하면 $9(4) + 2b = 4$, $2b = -32$, $b = -16$ 이다.

따라서 ㄱ, ㄴ, ㄷ 모두 옳다.

정답: ⑤
## 문제
길이가 `$1$`인 선분 `$AB$`가 있다. 그림과 같이 선분 `$AB$`를 `$3$`등분한 다음, 가운데 선분을 한 변으로 하는 정사각형을 그리고, 가운데 선분을 지워 만든 도형을 `$T_{1}$`이라 하자. 

`$T_{1}$`의 선분 중 원래의 선분 `$AB$`에서 남아 있는 두 선분을 각각 `$3$`등분한 다음, 가운데 선분을 한 변으로 하는 정사각형을 그리고, 가운데 선분을 지워 만든 도형을 `$T_{2}$`라 하자. `$T_{2}$`의 선분 중 원래의 선분 `$AB$`에서 남아 있는 네 선분을 각각 `$3$`등분한 다음, 가운데 선분을 한 변으로 하는 정사각형을 그리고, 가운데 선분을 지워 만든 도형을 `$T_{3}$`이라 하자. 

이와 같은 과정을 계속 반복하여 `$n$`번째 만든 도형을 `$T_{n}$`이라 하고, `$T_{n}$`에 있는 모든 선분의 길이의 총합을 `$a_{n}$`이라 하자. 이때 `$a_{20}$`의 값은?

> [!example] 📸 (그림/그래프)

## 보기
① `$3 \{ 1- (\frac{1}{3} )^{20} \}$`   ② `$3 \{ 1- (\frac{1}{3} )^{21} \}$`   ③ `$3 \{ 1- (\frac{2}{3} )^{19} \}$`   ④ `$3 \{ 1- (\frac{2}{3} )^{20} \}$`   ⑤ `$3 \{ 1- (\frac{2}{3} )^{21} \}$`

## 풀이
[정답] ⑤ 

`$a_{1} = 1 + \frac{1}{3} \times 2 = 1 + \frac{2}{3}$`
`$a_{2} = a_{1} + (\frac{1}{3})^{2} \times 2^{2} = 1 + \frac{2}{3} + (\frac{2}{3})^{2}$`
`$a_{3} = a_{2} + (\frac{1}{3})^{3} \times 2^{3} = 1 + \frac{2}{3} + (\frac{2}{3})^{2} + (\frac{2}{3})^{3}$`

`$\vdots$`

`$\therefore a_{20} = 1 + \frac{2}{3} + (\frac{2}{3})^{2} + \dots + (\frac{2}{3})^{20}$`
`$= \frac{1 - (\frac{2}{3})^{21}}{1 - \frac{2}{3}} = 3 \{ 1 - (\frac{2}{3})^{21} \}$`
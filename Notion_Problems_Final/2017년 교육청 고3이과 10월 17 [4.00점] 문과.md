## 문제
`$t < 1$` 인 실수 `$t$` 에 대하여 곡선 `$y = \ln x$` 와 직선 `$x + y = t$` 가 만나는 점을 `$P$` 라 하자. 점 `$P$` 에서 `$x$` 축에 내린 수선의 발을 `$H$`, 직선 `$PH$` 와 곡선 `$y = e^{x}$` 이 만나는 점을 `$Q$` 라 할 때, 삼각형 `$OHQ$` 의 넓이를 `$S(t)$` 라 하자. `$\lim_{t \to 0+} \frac{2S(t)-1}{t}$` 의 값은?

> [!example] 📸 (그림/그래프)

## 보기
① $1$   ② $e-1$   ③ $2$   ④ $e$   ⑤ $3$

## 풀이
점 `$P$` 의 좌표를 `$(a, \ln a)$` (`$a > 0$`)라 하면 점 `$Q$` 의 좌표는 `$(a, e^{a})$` 이다.
점 `$P$` 는 직선 `$x + y = t$` 위의 점이므로 `$a + \ln a = t$` 이다.
`$\ln a + a = t$` 이므로 `$\ln a + \ln e^{a} = t$`
`$\ln (a e^{a}) = t$` 이므로 `$a e^{a} = e^{t}$`
그러므로 삼각형 `$OHQ$` 의 넓이 `$S(t)$` 는
`$S(t) = \frac{1}{2} \times \overline{OH} \times \overline{HQ}$`
`$= \frac{1}{2} \times a \times e^{a}$`
`$= \frac{1}{2} a e^{a}$`
`$= \frac{1}{2} e^{t}$`

따라서
`$\lim_{t \to 0+} \frac{2S(t)-1}{t}$`
`$= \lim_{t \to 0+} \frac{2 \times \frac{1}{2} e^{t} - 1}{t}$`
`$= \lim_{t \to 0+} \frac{e^{t} - 1}{t}$`
`$= 1$`

정답: ①
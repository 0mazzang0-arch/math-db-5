## 문제
오른쪽 그림과 같이 원점 $O$를 지나는 원 $C$ 위에 원점이 아닌 서로 다른 두 점 $P$, $Q$가 있다. 일차변환 $f$에 의하여 점 $P$는 점 $Q$로, 점 $Q$는 원점 $O$로 옮겨질 때, 합성변환 $f \circ f$에 의하여 원 $C$는 어떤 도형으로 옮겨지는가?

## 보기
① 원점 $O$   ② 점 $P$   ③ 선분 $OQ$   ④ 호 $PQ$   ⑤ 원 $C$

## 풀이
일차변환을 나타내는 행렬을 $A$라 하고, 점 $P$, $Q$의 좌표를 $P(a, b)$, $Q(c, d)$라고 하면
$$ A \left( \begin{array}{c} a \\ b \end{array} \right) = \left( \begin{array}{c} c \\ d \end{array} \right), A \left( \begin{array}{c} c \\ d \end{array} \right) = \left( \begin{array}{c} 0 \\ 0 \end{array} \right) $$
또, 합성변환 $f \circ f$를 나타내는 행렬은 $A^{2}$이고
$$ A^{2} \left( \begin{array}{c} a \\ b \end{array} \right) = A \left( \begin{array}{c} c \\ d \end{array} \right) = \left( \begin{array}{c} 0 \\ 0 \end{array} \right), A^{2} \left( \begin{array}{c} c \\ d \end{array} \right) = A \left( \begin{array}{c} 0 \\ 0 \end{array} \right) = \left( \begin{array}{c} 0 \\ 0 \end{array} \right) $$
즉, 
$$ A^{2} \left( \begin{array}{cc} a & c \\ b & d \end{array} \right) = \left( \begin{array}{cc} 0 & 0 \\ 0 & 0 \end{array} \right) $$
한편, 세 점 $O$, $P$, $Q$는 일직선 위에 있지 않으므로 
$$ \frac{b}{a} \neq \frac{d}{c}, ad - bc \neq 0 $$
즉, 행렬 $\left( \begin{array}{cc} a & c \\ b & d \end{array} \right)$의 역행렬 $\left( \begin{array}{cc} a & c \\ b & d \end{array} \right)^{-1}$이 존재한다.
$$ \therefore A^{2} = \left( \begin{array}{cc} 0 & 0 \\ 0 & 0 \end{array} \right) \left( \begin{array}{cc} a & c \\ b & d \end{array} \right)^{-1} = \left( \begin{array}{cc} 0 & 0 \\ 0 & 0 \end{array} \right) $$
따라서 합성변환 $f \circ f$에 의하여 원 $C$는 원점 $O$로 옮겨진다.

정답: ①

> [!example] 📸 (그림/그래프)
## 문제
그림과 같이 좌표평면에 모든 꼭짓점이 좌표축 위에 있고 한 변의 길이가 각각 $\sqrt{2}$, $2\sqrt{2}$인 정사각형 $ABCD$와 정사각형 $EFGH$가 있다. 두 일차변환 $f$, $g$를 나타내는 행렬을 각각 $$ \left( \begin{array}{cc} k & 0 \\ 0 & k \end{array} \right), \left( \begin{array}{cc} 0 & -1 \\ -1 & 0 \end{array} \right) $$이라 하자. 합성변환 $g \circ f$에 의하여 점 $A$가 점 $H$로 옮겨질 때, 상수 $k$의 값은?

> [!example] 📸 (그림/그래프)

## 보기
① $-2$   ② $-\sqrt{2}$   ③ $-1$   ④ $\sqrt{2}$   ⑤ $2$

## 풀이
직각이등변삼각형 $OAB$에서 $\overline{AB} = \sqrt{2}$이므로 $\overline{OA} = 1$이다. 
$\therefore A(1, 0)$ 

직각이등변삼각형 $OGH$에서 $\overline{GH} = 2\sqrt{2}$이므로 $\overline{OH} = 2$이다. 
$\therefore H(0, -2)$ 

합성변환 $g \circ f$를 나타내는 행렬은 
$$ \left( \begin{array}{cc} 0 & -1 \\ -1 & 0 \end{array} \right) \left( \begin{array}{cc} k & 0 \\ 0 & k \end{array} \right) = \left( \begin{array}{cc} 0 & -k \\ -k & 0 \end{array} \right) $$
이고, 
$$ \left( \begin{array}{cc} 0 & -k \\ -k & 0 \end{array} \right) \left( \begin{array}{c} 1 \\ 0 \end{array} \right) = \left( \begin{array}{c} 0 \\ -k \end{array} \right) $$
이므로 합성변환 $g \circ f$에 의하여 점 $A(1, 0)$이 옮겨지는 점의 좌표는 $(0, -k)$이다. 

이 점이 점 $H(0, -2)$와 일치하므로 $-k = -2$에서 $k = 2$이다. 

정답: ⑤
## 문제
이차함수 $f(x) = x^{2} \log_{a} b + x \log_{a} b + \frac{1}{2}$ 이 임의의 실수 $x$ 에 대하여 $f(x) > 0$ 를 만족할 때, 점 $(a, b)$ 가 존재하는 영역을 어두운 부분으로 바르게 나타낸 것은? (단, $a$, $b$ 는 $1$ 이 아닌 양수이고 경계는 포함하지 않는다.)

## 보기
① > [!example] 📸 (그림/그래프)
② > [!example] 📸 (그림/그래프)
③ > [!example] 📸 (그림/그래프)
④ > [!example] 📸 (그림/그래프)
⑤ > [!example] 📸 (그림/그래프)

## 풀이
[정답] ② 

임의의 실수 $x$ 에 대하여 $f(x) > 0$ 가 성립하려면 
$\log_{a} b > 0$, $D = (\log_{a} b)^{2} - 2 \log_{a} b < 0$ 이여야 한다.

이를 정리하면
$\therefore 0 < \log_{a} b < 2$

(ⅰ) $0 < a < 1$ 인 경우 
$\log_{a} a^{0} < \log_{a} b < \log_{a} a^{2}$ 에서 부동호 방향이 바뀌므로
$\therefore a^{2} < b < 1$

(ⅱ) $a > 1$ 인 경우 
$\log_{a} a^{0} < \log_{a} b < \log_{a} a^{2}$ 에서
$\therefore 1 < b < a^{2}$

따라서 이를 만족하는 영역을 나타낸 것은 ②이다.
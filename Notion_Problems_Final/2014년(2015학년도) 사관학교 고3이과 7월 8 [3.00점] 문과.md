## 문제
함수 $$ f(x) = \left\{ \begin{array}{ll} -1 & ( x < -2 ) \\ x + 1 & ( -2 \le x < 2 ) \\ 3 & ( x \ge 2 ) \end{array} \right. $$ 가 있다. 집합 $\{ x \mid \frac{x+1}{f(x)} > x, |x| < 10, x \text{ 는 정수} \}$ 의 원소의 개수는? (단, 아래 그림은 두 함수 $y = f(x)$, $y = \frac{x+1}{x}$ 의 그래프를 나타낸 것이다.)

> [!example] 📸 (그림/그래프)

## 보기
① $3$   ② $5$   ③ $7$   ④ $9$   ⑤ $11$

## 풀이
[정답] ④

$x = -1$ 일 때, 함수 $f(x) = 0$ 이므로 분수식 $\frac{x+1}{f(x)}$ 가 정의되지 않는다.
(ⅰ) $x < -2$ 일 때,
$\frac{x+1}{f(x)} > x \iff \frac{x+1}{-1} > x \iff -(x+1) > x \iff x < -\frac{1}{2}$
$\therefore x < -2$
(ⅱ) $-2 \le x < 2$ 일 때,
$\frac{x+1}{f(x)} > x \iff \frac{x+1}{x+1} > x \iff 1 > x$
$\therefore -2 \le x < 1$, $x \neq -1$
(ⅲ) $x \ge 2$ 일 때,
$\frac{x+1}{f(x)} > x \iff \frac{x+1}{3} > x \iff x+1 < 3x \iff x < \frac{1}{2}$
$x \ge 2$ 범위에서 $\frac{x+1}{f(x)} > x$ 를 만족시키는 $x$ 는 존재하지 않는다.
(ⅰ), (ⅱ), (ⅲ)에서 부등식 $\frac{x+1}{f(x)} > x$ 의 해는 $x < 1$, $x \neq -1$ 이다.
따라서 $|x| < 10$ 인 정수 $x$ 의 집합은 $\{ -9, -8, \dots, -2, 0 \}$ 이다.
따라서 구하는 원소의 개수는 $9$ 개다.

[다른 풀이]
주어진 부등식의 양변에 $\frac{f(x)}{x}$ 를 곱하면 좌변은 $\frac{x+1}{x}$, 우변은 $f(x)$ 가 되므로 $x$ 와 $f(x)$ 의 부호가 바뀌는 $x$ 의 값을 기준으로 범위를 나누어 생각한다.
(ⅰ) $x < -1$ 일 때,
$x < 0$, $f(x) < 0$ 이므로 $\frac{x+1}{f(x)} > x \iff \frac{x+1}{x} > f(x)$
$x < -1$ 일 때, 함수 $y = f(x)$ 의 그래프는 함수 $y = \frac{x+1}{x}$ 의 그래프의 아래쪽에 있으므로 $\frac{x+1}{x} > f(x)$ 가 성립한다.
$\therefore x < -1$
(ⅱ) $-1 < x < 0$ 일 때,
$x < 0$, $f(x) > 0$ 이므로 $\frac{x+1}{f(x)} > x \iff \frac{x+1}{x} < f(x)$
$-1 < x < 0$ 일 때, 함수 $y = f(x)$ 의 그래프는 함수 $y = \frac{x+1}{x}$ 의 그래프의 위쪽에 있으므로 $\frac{x+1}{x} < f(x)$ 가 성립한다.
$\therefore -1 < x < 0$
(ⅲ) $x = 0$ 일 때,
$\frac{x+1}{f(x)} = 1$ 이므로 $\frac{x+1}{f(x)} > x$ 가 성립한다.
(ⅳ) $x > 0$ 일 때,
$x > 0$, $f(x) > 0$ 이므로 $\frac{x+1}{f(x)} > x \iff \frac{x+1}{x} > f(x)$
$y = f(x)$ 와 $y = \frac{x+1}{x}$ 의 그래프는 $x = 1$, $x = -1$ 일 때 만난다.

> [!example] 📸 (그림/그래프)

$0 < x < 1$ 일 때, 함수 $y = f(x)$ 의 그래프는 함수 $y = \frac{x+1}{x}$ 의 그래프의 아래쪽에 있으므로 $\frac{x+1}{x} > f(x)$ 가 성립한다.
$\therefore 0 < x < 1$
(ⅰ), (ⅱ), (ⅲ), (ⅳ)에서 주어진 집합은 $x < 1$ 이고 $x \neq -1$ 이며 $|x| < 10$ 을 만족시키는 정수 $x$ 의 집합으로 $\{ -9, -8, \dots, -2, 0 \}$ 이다.
따라서 구하는 원소의 개수는 $9$ 개다.
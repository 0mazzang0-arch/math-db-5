## 문제
두 부등식 $x > 0$, $y > x$ 의 영역에 속하는 점 $P(a, b)$ 에서 두 직선 $y = x$, $y = -x$ 에 이르는 거리를 각각 $c$, $d$ 라 하자. 이차정사각행렬 $M$ 이 $M \begin{pmatrix} a \\ b \end{pmatrix} = \begin{pmatrix} c \\ d \end{pmatrix}$ 를 만족할 때, 행렬 $M + M^{-1}$ 의 모든 성분의 합은?



> [!example] 📸 (그림/그래프)

## 보기
① $1$   ② $\sqrt{2}$   ③ $2$   ④ $2 \sqrt{2}$   ⑤ $3$

## 풀이
점 $P(a, b)$ 에서 두 직선 $x - y = 0$ 와 $x + y = 0$ 에 이르는 거리는 다음과 같다.
$$ \begin{array}{l} c = \displaystyle\frac{|a - b|}{\sqrt{1^2 + (-1)^2}} = \frac{|a - b|}{\sqrt{2}} \\ d = \displaystyle\frac{|a + b|}{\sqrt{1^2 + 1^2}} = \frac{|a + b|}{\sqrt{2}} \end{array} $$

조건에서 $x > 0$, $y > x$ 이므로 $a > 0$, $b > a$ 이다. 따라서 $a - b < 0$ 이고 $a + b > 0$ 이다.
$$ \begin{array}{l} c = \displaystyle\frac{b - a}{\sqrt{2}} = -\frac{1}{\sqrt{2}} a + \frac{1}{\sqrt{2}} b \\ d = \displaystyle\frac{a + b}{\sqrt{2}} = \frac{1}{\sqrt{2}} a + \frac{1}{\sqrt{2}} b \end{array} $$

이를 행렬 $M \begin{pmatrix} a \\ b \end{pmatrix} = \begin{pmatrix} c \\ d \end{pmatrix}$ 로 나타내면 다음과 같다.
$$ M \begin{pmatrix} a \\ b \end{pmatrix} = \left( \begin{array}{cc} -\frac{1}{\sqrt{2}} & \frac{1}{\sqrt{2}} \\ \frac{1}{\sqrt{2}} & \frac{1}{\sqrt{2}} \end{array} \right) \begin{pmatrix} a \\ b \end{pmatrix} = \begin{pmatrix} c \\ d \end{pmatrix} $$

따라서 행렬 $M$ 은 다음과 같다.
$$ M = \left( \begin{array}{cc} -\frac{1}{\sqrt{2}} & \frac{1}{\sqrt{2}} \\ \frac{1}{\sqrt{2}} & \frac{1}{\sqrt{2}} \end{array} \right) $$

행렬 $M$ 의 행렬식 $\det(M) = (-\frac{1}{\sqrt{2}})(\frac{1}{\sqrt{2}}) - (\frac{1}{\sqrt{2}})(\frac{1}{\sqrt{2}}) = -\frac{1}{2} - \frac{1}{2} = -1$ 이므로 역행렬 $M^{-1}$ 은 다음과 같다.
$$ M^{-1} = \frac{1}{-1} \left( \begin{array}{cc} \frac{1}{\sqrt{2}} & -\frac{1}{\sqrt{2}} \\ -\frac{1}{\sqrt{2}} & -\frac{1}{\sqrt{2}} \end{array} \right) = \left( \begin{array}{cc} -\frac{1}{\sqrt{2}} & \frac{1}{\sqrt{2}} \\ \frac{1}{\sqrt{2}} & \frac{1}{\sqrt{2}} \end{array} \right) = M $$

따라서 $M + M^{-1} = 2M$ 이다.
$$ M + M^{-1} = 2 \left( \begin{array}{cc} -\frac{1}{\sqrt{2}} & \frac{1}{\sqrt{2}} \\ \frac{1}{\sqrt{2}} & \frac{1}{\sqrt{2}} \end{array} \right) = \left( \begin{array}{cc} -\sqrt{2} & \sqrt{2} \\ \sqrt{2} & \sqrt{2} \end{array} \right) $$

행렬 $M + M^{-1}$ 의 모든 성분의 합은 다음과 같다.
$$ -\sqrt{2} + \sqrt{2} + \sqrt{2} + \sqrt{2} = 2 \sqrt{2} $$

정답은 ④ 이다.
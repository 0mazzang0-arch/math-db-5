## 문제
$0 < a < b < 1$ 일 때, 직선 $y = 1$ 이 $y = \log_a x$ 의 그래프와 $y = \log_b x$ 의 그래프와 만나는 점을 각각 $P$, $Q$ 라 하고 직선 $y = -1$ 이 $y = \log_a x$ 의 그래프와 $y = \log_b x$ 의 그래프와 만나는 점을 각각 $R$, $S$ 라 하자. $PS$, $PR$, $QS$, $QR$ 의 기울기를 각각 $\alpha$, $\beta$, $\gamma$, $\delta$ 라 할 때, 다음 중 옳은 것은?

## 보기
① $\delta < \alpha < \beta < \gamma$   ② $\gamma < \alpha < \delta < \beta$   ③ $\gamma < \alpha < \beta < \delta$   ④ $\gamma < \alpha = \delta < \beta$   ⑤ $\alpha = \delta < \beta < \gamma$

## 풀이
[정답] ②

함수 $y = \log_a x$, $y = \log_b x$ 의 그래프와 직선 $y = 1$, $y = -1$ 를 그려 점 $P$, $Q$, $R$, $S$ 를 표시하면 다음과 같다.

> [!example] 📸 (그림/그래프)

위 그림에서 $\overline{PQ} < \overline{SR}$ 이므로 $\gamma < \alpha < \delta < \beta$ 이다.
$0 < a < b < 1$ 일 때, 직선 $y = 1$ 이 $y = \log_a x$ 의 그래프와 $y = \log_b x$ 의 그래프와 만나는 점을 각각 $P(a, 1)$, $Q(b, 1)$ 라 하고 직선 $y = -1$ 이 $y = \log_a x$ 의 그래프와 $y = \log_b x$ 의 그래프와 만나는 점을 각각 $R(1/a, -1)$, $S(1/b, -1)$ 라 하자.

각 선분의 기울기는 다음과 같다.
$\alpha (\text{직선 } PS \text{의 기울기}) = \frac{-1 - 1}{1/b - a} = \frac{-2}{1/b - a}$
$\beta (\text{직선 } PR \text{의 기울기}) = \frac{-1 - 1}{1/a - a} = \frac{-2}{1/a - a}$
$\gamma (\text{직선 } QS \text{의 기울기}) = \frac{-1 - 1}{1/b - b} = \frac{-2}{1/b - b}$
$\delta (\text{직선 } QR \text{의 기울기}) = \frac{-1 - 1}{1/a - b} = \frac{-2}{1/a - b}$

분모의 크기를 비교하면 $1/b - b < 1/b - a < 1/a - b < 1/a - a$ 이므로, 그 역수에 $-2$를 곱한 기울기의 값은 $\gamma < \alpha < \delta < \beta$ 이 된다.
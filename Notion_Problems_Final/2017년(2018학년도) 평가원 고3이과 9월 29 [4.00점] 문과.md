## 문제
좌표공간에 세 점 `$O(0, 0, 0)$`, `$A(1, 0, 0)$`, `$B(0, 0, 2)$`가 있다. 점 `$P$`가 `$\vec{OB} \cdot \vec{OP} = 0$`, `$|\vec{OP}| \le 4$`를 만족시키며 움직일 때, `$|\vec{PQ}| = 1$`, `$\vec{PQ} \cdot \vec{OA} \ge \frac{\sqrt{3}}{2}$`을 만족시키는 점 `$Q$`에 대하여 `$|\vec{BQ}|$`의 최댓값과 최솟값을 각각 `$M$`, `$m$`이라 하자. `$M + m = a + b\sqrt{5}$`일 때, `$6(a + b)$`의 값을 구하시오. (단, `$a$`, `$b$`는 유리수이다.)

## 보기

## 풀이
점 `$P$`가 `$\vec{OB} \cdot \vec{OP} = 0$`, `$|\vec{OP}| \le 4$`를 만족시키므로 점 `$P$`는 `$xy$` 평면 위에 있으며 중심이 원점이고 반지름의 길이가 `$4$`인 원의 경계 및 내부이다. 
또, 점 `$Q$`가 `$|\vec{PQ}| = 1$`이고, `$\vec{PQ} \cdot \vec{OA} \ge \frac{\sqrt{3}}{2}$`이므로 두 벡터 `$\vec{PQ}$`, `$\vec{OA}$`가 이루는 각의 크기를 `$\theta$`라 하면 

`$|\vec{PQ}||\vec{OA}| \cos \theta \ge \frac{\sqrt{3}}{2}$` 

`$\cos \theta \ge \frac{\sqrt{3}}{2}$` 

`$0 \le \theta \le \frac{\pi}{6}$` 

그러므로 점 `$Q$`는 점 `$P$`를 중심으로 하고 반지름의 길이가 `$1$`인 구 위에 있으며 벡터 `$\vec{OA}$`, `$\vec{PQ}$`가 이루는 각의 크기 `$\theta$`가 `$0 \le \theta \le \frac{\pi}{6}$`를 만족시키는 점으로 그림과 같다. 

> [!example] 📸 (그림/그래프)

이때, 벡터 `$\vec{BQ}$`의 크기가 최대이려면 점 `$P$`가 `$C(4, 0, 0)$`일 때이고 다음 그림과 같이 두 점 `$B$`, `$C$`를 지나는 직선이 중심이 `$C$`이고 반지름의 길이가 `$1$`인 구와 만나는 점일 때이다. 

> [!example] 📸 (그림/그래프)

그러므로 `$|\vec{BQ}|$`의 최댓값은 

`$M = \overline{BC} + \overline{CQ}$` 

`$= \sqrt{(4 - 0)^{2} + (0 - 0)^{2} + (0 - 2)^{2}} + 1$` 

`$= 2\sqrt{5} + 1$` 

또, 벡터 `$\vec{BQ}$`의 크기가 최소이려면 그림과 같이 점 `$P$`가 `$D\left( -\frac{\sqrt{3}}{2}, 0, 0 \right)$`일 때이고 점 `$Q$`는 중심이 `$D$`이고 반지름의 길이가 `$1$`인 구가 `$z$`축과 만나는 점일 때이다. 

> [!example] 📸 (그림/그래프)

그러므로 `$|\vec{BQ}|$`의 최솟값은 `$\overline{OQ} = \frac{1}{2}$`이므로 

`$m = \overline{BQ}$` 

`$= 2 - \frac{1}{2}$` 

`$= \frac{3}{2}$` 

따라서 

`$M + m = (1 + 2\sqrt{5}) + \frac{3}{2}$` 

`$= \frac{5}{2} + 2\sqrt{5}$` 

`$a = \frac{5}{2}$`, `$b = 2$`이므로

`$6(a + b) = 6 \times \left( \frac{5}{2} + 2 \right)$` 

`$= 27$`
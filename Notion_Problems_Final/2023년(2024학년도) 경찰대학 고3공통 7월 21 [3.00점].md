## 문제
최고차항의 계수가 $1$ 인 삼차함수 $f(x)$ 에 대하여 함수 $g(x)$ 를
$$ g(x) = \begin{cases} f(x) & (x < 1) \\ -f(x) & (x \ge 1) \end{cases} $$
이라 하자. 함수 $g(x)$ 가 실수 전체의 집합에서 미분가능하고 $x = -1$ 에서 극값을 가질 때, 함수 $f(x)$ 의 극댓값을 구하시오.

## 풀이
삼차함수 $f(x)$ 의 $x^3$ 의 계수는 $1$ 이고, 함수 $g(x)$ 는 모든 실수에서 미분가능하므로 $x = 1$ 에서 미분가능하다.
함수 $g(x) = \begin{cases} f(x) & (x < 1) \\ -f(x) & (x \ge 1) \end{cases}$ 이고 $x = 1$ 에서 연속이므로
$\lim_{x \to 1^-} g(x) = \lim_{x \to 1^+} g(x) = g(1)$ 에서
$\lim_{x \to 1^-} f(x) = \lim_{x \to 1^+} \{ -f(x) \} = f(1)$
즉 $-f(1) = f(1)$ 이므로 $f(1) = 0$, $g(1) = 0$
또 함수 $g(x)$ 가 $x = 1$ 에서 미분가능하므로
$\lim_{x \to 1^-} \frac{g(x) - g(1)}{x - 1} = \lim_{x \to 1^+} \frac{g(x) - g(1)}{x - 1}$ 에서
$\lim_{x \to 1^-} \frac{f(x) - f(1)}{x - 1} = \lim_{x \to 1^+} \frac{-f(x) - f(1)}{x - 1}$
즉 $f'(1) = -f'(1)$ 이므로 $f'(1) = 0$
$f(1) = 0$, $f'(1) = 0$ 이므로 $f(x)$ 는 $(x - 1)^2$ 을 인수로 갖는다.
$f'(x) = 3(x - 1)(x - a)$ ($a$ 는 상수)라 하면
$$ g'(x) = \begin{cases} f'(x) & (x < 1) \\ -f'(x) & (x > 1) \end{cases} = \begin{cases} 3(x - 1)(x - a) & (x < 1) \\ -3(x - 1)(x - a) & (x > 1) \end{cases} $$
(ⅰ) $a < 1$ 일 때
$g(x)$ 의 도함수 $y = g'(x)$ 의 그래프의 개형은 그림과 같다.

> [!example] 📸 (그림/그래프)

$g(x)$ 는 $x = a$ 에서 극대이므로 조건에 의하여 $a = -1$

(ⅱ) $a = 1$ 일 때
$g(x)$ 의 도함수 $y = g'(x)$ 의 그래프의 개형은 그림과 같다.

> [!example] 📸 (그림/그래프)

$g(x)$ 는 $x = 1$ 에서만 극값을 가지므로 조건을 만족시키지 않는다.

(ⅲ) $a > 1$ 일 때
$g(x)$ 의 도함수 $y = g'(x)$ 의 그래프의 개형은 그림과 같다.

> [!example] 📸 (그림/그래프)

$g(x)$ 는 $x = a$ 에서 극대이고 $a > 1$ 이므로 조건을 만족시키지 않는다.

이상에서 함수 $g(x)$ 가 $x = -1$ 에서 극값을 갖도록 하는 실수 $a$ 의 값은 $-1$ 이므로
$f'(x) = 3(x - 1)(x + 1) = 3x^2 - 3$
$\therefore f(x) = x^3 - 3x + 2$ ($\because f(1) = 0$)
함수 $f(x)$ 의 증가와 감소를 표로 나타내면 다음과 같다.
$$ \begin{array}{c|c|c|c|c|c} x & \cdots & -1 & \cdots & 1 & \cdots \\ \hline f'(x) & + & 0 & - & 0 & + \\ \hline f(x) & \nearrow & 4 & \searrow & 0 & \nearrow \end{array} $$
즉 함수 $f(x)$ 는 $x = -1$ 에서 극댓값 $4$ 를 갖고, $x = 1$ 에서 극솟값 $0$ 을 갖는다.
따라서 함수 $f(x)$ 의 극댓값은 $4$ 이다.
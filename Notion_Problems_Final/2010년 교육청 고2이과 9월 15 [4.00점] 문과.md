## 문제

> [!example] 📸 (그림/그래프)

`[그림1]`은 아르키메데스의 저서에 있는 셀리논(`$Salinon$`) 이라는 도형이다. 아르키메데스는 셀리논을 다음과 같은 방법으로 그렸다.

> [!example] 📸 (그림/그래프)

`[그림1]`

> [!example] 📸 (그림/그래프)

`[그림2]`

(i) 선분 `$\overline{AB}$`를 지름으로 하는 반원 `$ACB$`를 그린다.
(ii) 지름 `$\overline{AB}$` 위에 `$A$`와 `$B$`로부터 각각 같은 거리만큼 떨어진 점 `$D$`, `$E$`를 잡는다.
(iii) 선분 `$\overline{AD}$`와 선분 `$\overline{EB}$`를 지름으로 하는 두 개의 반원을 반원 `$ACB$`와 같은 쪽으로 그린다.
(iv) 선분 `$\overline{DE}$`를 지름으로 하는 반원을 반원 `$ACB$`와 반대쪽에 그린다.

`[그림2]`에서 `$\overline{AB}=10$`이고, `$\overline{DE}$`, `$\overline{AD}$`, `$\overline{CF}$`가 이 순서대로 등차수열을 이룰 때, 이 셀리논의 넓이는?
(단, `$\overline{AO}=\overline{BO}$`, `$\overline{CF}$`는 `$\overline{AB}$`의 수직이등분선)

## 보기

① $7 \pi$   ② $\frac{15}{2} \pi$   ③ $8 \pi$   ④ $\frac{17}{2} \pi$   ⑤ $9 \pi$

## 풀이

`$\overline{AB}=10$`이므로 
`$4 r_{1}+2 r_{2}=10$` 

`$\overline{DE}=2 r_{2}$`, 
`$\overline{AD}=2 r_{1}$`, 
`$\overline{CF}=2 r_{1}+2 r_{2}$`가 차례로 등차수열을 이루므로 
`$4 r_{1}=2 r_{1}+4 r_{2}$` 

`$\therefore r_{1}=2$`, 
`$r_{2}=1$` 

셀리논(`$S$`)의 넓이 `$S$`는 
`$S=\frac{1}{2} \times 5^{2} \pi-2 \times \frac{1}{2} \times 4 \pi+\frac{1}{2} \pi=9 \pi$`
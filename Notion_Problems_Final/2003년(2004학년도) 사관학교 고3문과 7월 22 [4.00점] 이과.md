## 문제
한국 오페라하우스의 좌석은 `$A$` $\sim$ `$G$` 까지 `$7$` 개의 열로 구성되어 있고, 각 열에는 `$55$` 개의 행이 같은 규칙으로 배열되어 있다. 예를 들어, `$A$` 열의 행들이 아래와 같이 배열되어 있을 때, 한국 오페라하우스의 총 좌석 수는?

> [!example] 📸 (그림/그래프)

## 보기
① `$3,542$`   ② `$4,235$`   ③ `$5,733$`   ④ `$11,172$`   ⑤ `$21,175$`

## 풀이
[정답] ② 

`$A$` 열의 총 좌석수를 `$S$` 라 두면 

`$S = 1 \times 5 + 2 \times 6 + 3 \times 7 + \cdots + 10 \times 14$` 

`$= \sum_{n=1}^{10} n(n+4) = \sum_{n=1}^{10} (n^{2}+4n)$` 

`$= \frac{10 \cdot 11 \cdot 21}{6} + 4 \times \frac{10 \cdot 11}{2} = 605$` 

그러므로 총 좌석수는 

`$\therefore 605 \times 7 = 4,235$`
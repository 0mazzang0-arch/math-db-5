## 문제

다음과 같이 정사각형을 가로 방향으로 `$3$`등분하여 [도형 `$1$`]을 만들고, 세로 방향으로 `$3$`등분하여 [도형 `$2$`]를 만든다.

> [!example] 📸 (그림/그래프)

> [!example] 📸 (그림/그래프)

[도형 `$1$`] [도형 `$2$`]

[도형 `$1$`]과 [도형 `$2$`]를 번갈아 가며 계속 붙여 아래와 같은 도형을 만든다. 그림과 같이 첫 번째 붙여진 [도형 `$1$`]의 왼쪽 맨 위 꼭짓점을 `$A$`라 하고, [도형 `$1$`]의 개수와 [도형 `$2$`]의 개수를 합하여 `$n$`개 붙여 만든 도형의 오른쪽 맨 아래 꼭짓점을 `$B_{n}$`이라 하자.

> [!example] 📸 (그림/그래프)

꼭짓점 `$A$`에서 꼭짓점 `$B_{n}$`까지 선을 따라 최단거리로 가는 경로의 수를 `$a_{n}$`이라 할 때, `$a_{3} + a_{7}$`의 값은?

## 보기

① $26$   ② $28$   ③ $30$   ④ $32$   ⑤ $34$

## 풀이

(ⅰ) `$n$`이 짝수일 때,

> [!example] 📸 (그림/그래프)

`$B_{n}$`에 이르는 최단경로는
`$A \to B_{n-1} \to B_{n}$`
`$A \to P \to B_{n}$`

`$a_{n} = a_{n-1} \times 1 + 1 \times \frac{3!}{2!1!}$`
`$= a_{n-1} + 3$`

(ⅱ) `$n$`이 홀수일 때,

> [!example] 📸 (그림/그래프)

`$B_{n}$`에 이르는 최단경로는
`$A \to B_{n-1} \to B_{n}$`
`$A \to Q \to R \to B_{n}$`

`$a_{n} = a_{n-1} \times 1 + 1 \times \frac{3!}{2!1!} \times 1$`
`$= a_{n-1} + 3$`

(ⅰ), (ⅱ)에서
`$a_{n} = a_{n-1} + 3$` (`$n=2, 3, 4, \cdots$`)
이므로 수열 `$\{a_{n}\}$`은 공차가 `$3$`인 등차수열이다.
`$a_{1} = \frac{4!}{3!1!} = 4$`이므로

`$a_{3} = 4 + 2 \times 3 = 10$`,
`$a_{7} = 4 + 6 \times 3 = 22$`

`$\therefore a_{3} + a_{7} = 32$`

정답: ④
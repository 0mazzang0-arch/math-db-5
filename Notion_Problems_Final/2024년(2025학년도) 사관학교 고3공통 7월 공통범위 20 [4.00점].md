## 문제

최고차항의 계수가 `$1$`인 삼차함수 `$f(x)$`와 함수 `$g(x) = |f(x)|$`가 다음 조건을 만족시킬 때, `$g(8)$`의 값을 구하시오.

(가) 함수 `$y=f'(x)$`의 그래프는 직선 `$x=2$`에 대하여 대칭이다.
(나) 함수 `$g(x)$`는 `$x=5$`에서 미분가능하고, `$y=g(x)$` 위의 점 `$(5, g(5))$`에서의 접선은 곡선 `$y=g(x)$`와 점 `$(0, g(0))$`에서 접한다.

## 보기


## 풀이

최고차항의 계수가 `$1$`인 삼차함수 `$f(x)$`에서 조건 (가)에 의하여 `$y=f'(x)$`의 그래프가 직선 `$x=2$`에 대하여 대칭이므로
`$$f'(x) = 3(x-2)^2 + a$$`
라 하면
`$$f(x) = (x-2)^3 + ax + b \text{ ($a$, $b$는 상수)}$$`
이때 삼차함수 `$f(x)$`의 극값이 존재하지 않거나 극솟값이 `$0$` 이상일 경우 다음 그림과 같이 조건 (나)를 만족하지 않으므로 곡선 `$y=g(x)$` 위의 점에서의 접선을 `$y=h(x)$`라 하면 함수 `$y=g(x)$`의 그래프는 다음과 같다.

> [!example] 📸 (그림/그래프)

`$f'(0) = -f'(5)$`이므로
`$f'(0) = 12+a$`, `$f'(5) = 27+a$`
`$12+a = -27-a$`
`$\therefore$` `$a = -\frac{39}{2}$`

직선 `$y=h(x)$`는 두 점 `$(0, f(0))$`, `$(5, -f(5))$`를 지나므로 직선 `$y=h(x)$`의 기울기는
`$$ \frac{-f(5)-f(0)}{5-0} = \frac{-b+\frac{141}{2}+8-b}{5-0} = \frac{-2b+\frac{157}{2}}{5} $$`
`$f'(0) = 12-\frac{39}{2} = -\frac{15}{2}$`에서
`$$ \frac{-2b+\frac{157}{2}}{5} = -\frac{15}{2} $$`
`$\therefore$` `$b=58$`

`$\therefore$` `$g(8) = |f(8)|$`
`$$= |216-156+58|$$`
`$$= 118$$`

정답: `$118$`
## 문제
부등식 $\log_{2} (x-1) < 5$ 를 만족시키는 모든 자연수 $x$ 의 개수를 구하시오.

## 보기


## 풀이
$\log_{2} (x-1) < \log_{2} 2^{5}$ 이고 
$y = \log_{2} (x-1)$ 은 $x$ 의 값이 증가할 때 
$y$ 의 값이 증가하므로 
$x-1 < 32$ 이다. 
그러므로 $x < 33$ 이고 로그의 정의에서 
$x-1 > 0$ 이므로 $x > 1$ 이다. 
따라서 $1 < x < 33$ 이므로 자연수 $x$ 의 개수는 $31$ 이다.

정답: $31$
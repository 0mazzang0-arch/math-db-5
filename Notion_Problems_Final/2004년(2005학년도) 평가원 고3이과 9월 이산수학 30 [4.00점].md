## 문제

> [!example] 📸 (그림/그래프)

그림과 같이 `$x$` 축 위의 두 점 `$A(20, 0)$`, `$B(80, 0)$` 와 양의 `$y$` 축 위의 점 `$P(0, y)$` 에 대하여 `$\angle APB = \theta$` 라고 할 때, `$\tan \theta$` 의 값이 최대가 되는 점 `$P$` 의 `$y$` 좌표를 구하시오.

## 풀이

**[정답]**
`$40$`

`$\theta = \alpha - \beta$`

> [!example] 📸 (그림/그래프)

이므로 
`$$\tan \theta = \tan (\alpha - \beta)$$`
`$$= \frac{\tan \alpha - \tan \beta}{1 + \tan \alpha \cdot \tan \beta}$$`
`$$\tan (\alpha - \beta) = \frac{\frac{y}{20} - \frac{y}{80}}{1 + \frac{y}{20} \cdot \frac{y}{80}}$$`
`$$= \frac{60y}{1600 + y^2}$$`
`$$= \frac{60}{\frac{1600}{y} + y}$$`
분모가 가장 작을 때 `$\tan \theta$` 는 최대이므로
`$$\frac{1600}{y} + y \geq 2 \sqrt{\frac{1600}{y} \cdot y}$$`
`$\therefore \frac{1600}{y} = y$` 일 때 분모는 최소이다. 
`$\therefore y = 40$`

**[다른 풀이]**

`$A$`, `$B$`

> [!example] 📸 (그림/그래프)

두 점을 지나고 `$y$` 축에 접할 때, `$\theta$` 가 최대이고 이 때 `$\tan \theta$` 도 최대이다. 
그런데 
`$$\overline{OP}^2 = \overline{OA} \cdot \overline{OB}$$`
`$$= 1600$$`
`$\therefore \overline{OP} = 40$`
## 문제

두 종류의 연산 장치

> [!example] 📸 (그림/그래프)

와 

> [!example] 📸 (그림/그래프)

가 있다. `$2$` 이상의 두 자연수 `$a$`, `$b$`에 대하여 연산 장치

> [!example] 📸 (그림/그래프)

에 두 수 `$a$`, `$b$`가 입력될 때 출력되는 수 `$p$`는 `$p= \log_{a} b$`이고, 연산 장치

> [!example] 📸 (그림/그래프)

에 두 수 `$a$`, `$b$`가 입력될 때 출력되는 수 `$q$`는 `$q= \sqrt[a]{b}$`이다. 

위의 두 종류의 연산 장치를 결합하여 만든 장치에서 각 연산 장치를 거쳐 최종적으로 출력되는 세 수 `$x$`, `$y$`, `$z$`의 대소 관계를 옳게 나타낸 것은?

> [!example] 📸 (그림/그래프)

## 보기
① $x<y<z$   ② $x<z<y$   ③ $y<x<z$   ④ $y<z<x$   ⑤ $z<x<y$

## 풀이
[정답] ③ 

`$k= \log_{2} 64=6$` 이므로 
`$x= \sqrt[4]{6}= \sqrt[12]{216}$` 

`$l= \sqrt{36}=6$` 이므로 
`$y= \sqrt[6]{13}= \sqrt[12]{169}$` 

`$m= \log_{2} 8=3$`, 
`$n= \log_{3} 81=4$` 이므로 

`$z= \sqrt[3]{4}= \sqrt[12]{256}$` 

`$\therefore y<x<z$`
## 문제
`$x$` 에 대한 다항식 `$x^{3}-x^{2}-10x+a$` 가 `$x-1$` 로 나누어떨어질 때, 상수 `$a$` 의 값을 구하시오.

## 보기

## 풀이
`$f(x)=x^{3}-x^{2}-10x+a$` 라 하면 `$f(x)$` 가 `$x-1$` 로 나누어떨어지므로 인수정리에 의하여
`$$ f(1)=1^{3}-1^{2}-10 \times 1+a=0 $$`
따라서 `$a=10$` 이다.

**정답: $10$**
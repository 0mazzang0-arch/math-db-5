## 문제
최고차항의 계수가 $1$이고, $f(0)=3$, $f'(3)<0$인 사차함수 $f(x)$가 있다. 실수 $t$에 대하여 집합 $S$를
$S = \{ a \mid \text{함수 } |f(x)-t| \text{ 가 } x=a \text{ 에서 미분가능하지 않다.} \}$
라 하고, 집합 $S$의 원소의 개수를 $g(t)$라 하자. 함수 $g(t)$가 $t=3$과 $t=19$에서만 불연속일 때, $f(-2)$의 값을 구하시오.

## 보기

## 풀이
$|f(x)-t|$는 $f(x)-t$ ($f(x) \ge t$) 또는 $-(f(x)-t)$ ($f(x) < t$)이다.
$f(x)$와 $y=t$의 그래프에서 $y=t$의 아래쪽 $f(x)$를 $y=t$에 선대칭해서 그려주면 된다. 꺾인 그래프에서 첨점의 개수가 미분불가능한 점이므로 그 개수를 세어주면 된다.
따라서 최고차항의 계수가 $1$인 사차함수의 그래프 개형을 그리면 아래와 같다.

$1$)
> [!example] 📸 (그림/그래프)
$t > f(\alpha)$이면 $g(t)=2$, $t \le f(\alpha)$이면 $g(t)=0$이므로 불연속점이 $1$개이므로 조건에 맞지 않다.

$2$)
> [!example] 📸 (그림/그래프)
$1$)번과 동일하므로 조건에 맞지 않다.

$3$)
> [!example] 📸 (그림/그래프)
$t > f(\beta)$이면 $g(t)=2$, $t=f(\beta)$이면 $g(t)=1$
$f(\alpha) < t < f(\beta)$이면 $g(t)=2$, $t \le f(\alpha)$이면 $g(t)=0$
불연속이 두 개인 조건에는 맞지만 $f(\beta)=19$, $f(\alpha)=3$이므로 $\alpha=3$이다.
그런데 $f'(3) < 0$이라는 조건에는 모순이므로 조건에 맞지 않다.

$4$)
> [!example] 📸 (그림/그래프)
$t \ge f(\beta)$이면 $g(t)=2$, $f(\gamma) \le t < f(\beta)$이면 $g(t)=4$
$f(\alpha) < t < f(\gamma)$이면 $g(t)=2$, $t \le f(\alpha)$이면 $g(t)=0$
$t=\alpha, \beta, \gamma$에서 불연속이므로 문제 조건에 맞지 않다.

$5$)
> [!example] 📸 (그림/그래프)
$t \ge f(\beta)$이면 $g(t)=2$, $f(\alpha)=f(\gamma) \le t < f(\beta)$이면 $g(t)=4$,
$t \le f(\alpha)=f(\gamma)$이면 $g(t)=0$
$t=\alpha, \beta$에서 불연속이므로 문제 조건에 맞다.

$f'(3) < 0$이므로 $\alpha=0$, $f(\alpha)=f(\gamma)=3$, $f(\beta)=19$이므로
$f(x) = x^{2}(x-\gamma)^{2}+3$, $f'(\beta)=0$이므로 $\beta = \frac{\gamma}{2}$
$f(\beta) = f\left(\frac{\gamma}{2}\right) = 19$에서 $\gamma=4, \beta=2$
$\therefore f(x) = x^{2}(x-4)^{2}+3$
$f(-2) = (-2)^{2}(-2-4)^{2}+3 = 4 \times 36 + 3 = 147$

정답: $147$
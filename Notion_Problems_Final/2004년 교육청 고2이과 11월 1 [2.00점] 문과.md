## 문제
`$log _{a} 3 = \frac{5}{2}$` 일 때, `$a^{5}$` 의 값은?
(단, `$a > 0$`, `$a \neq 1$`)

## 보기
① $2$   ② $3$   ③ $5$   ④ $9$   ⑤ $10$

## 풀이
`$log _{a} 3 = \frac{5}{2}$` 에서 로그의 정의에 의해
`$3 = a^{\frac{5}{2}}$` 이다.
양변을 제곱하면
`$3^{2} = (a^{\frac{5}{2}})^{2}$`
`$9 = a^{5}$`
따라서 `$a^{5} = 9$` 이다.

정답: ④
## 문제
양수 `$a$`에 대하여 점 `$(a, 0)$`에서 곡선 `$y=3x^{3}$`에 그은 접선과 점 `$(0, a)$`에서 곡선 `$y=3x^{3}$`에 그은 접선이 서로 평행할 때, `$90a$`의 값을 구하시오.

## 보기

## 풀이
`$y=3x^{3}$`은 기함수이므로 같은 기울기를 가지는 접점 `$P$`, `$Q$`는 원점 대칭이다. 따라서 `$P(t, 3t^{3})$`이면 `$Q(-t, -3t^{3})$`이다.

> [!example] 📸 (그림/그래프)

점 `$P$`에서의 접선의 방정식은
`$y=9t^{2}(x-t)+3t^{3}=9t^{2}x-6t^{3}$` $\cdots$ ㉠
㉠이 점 `$A(a, 0)$`을 지나므로
`$0=9t^{2}a-6t^{3}$` $\cdots$ ㉡

점 `$Q$`에서의 접선의 방정식은
`$y=9t^{2}(x+t)-3t^{3}=9t^{2}x+6t^{3}$` $\cdots$ ㉢
㉢이 점 `$B(0, a)$`를 지나므로
`$a=6t^{3}$` $\cdots$ ㉣

㉡, ㉣에서
`$9t^{2}a = a$` 이고, `$a > 0$`이므로
`$9t^{2}=1 \therefore t=\frac{1}{3}$`

`$a=6 \times \left(\frac{1}{3}\right)^{3}=6 \times \frac{1}{27}=\frac{2}{9}$`
`$\therefore 90a = 90 \times \frac{2}{9} = 20$`

정답: `$20$`